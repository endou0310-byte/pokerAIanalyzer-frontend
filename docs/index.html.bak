<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Poker Analyzer - Login</title>
  <link rel="stylesheet" href="./css/style.css">
  <script src="./js/config.js"></script>
</head>
<body>
  <div class="container">
    <h1>🂡 Poker Analyzer</h1>
    <div class="section">
      <h2>ログイン</h2>
      <button id="loginBtn">ログイン（テスト）</button>
      <div style="margin-top:12px;">
        <input id="userId" readonly placeholder="user_id">
      </div>
      <div style="margin-top:8px;">
        <input id="userName" readonly placeholder="display_name / email">
      </div>
    </div>
    <div class="section" style="text-align:center;">
      <button id="toDashboard">ダッシュボードへ ▶</button>
    </div>
  </div>

  <script type="module">
    import { loginDummy } from "./js/api.js";
    import { Storage } from "./js/storage.js";

    document.getElementById("loginBtn").addEventListener("click", async () => {
      const res = await loginDummy();
      if (!res.ok) return alert("ログイン失敗");
      Storage.setUser(res);
      document.getElementById("userId").value = res.user_id;
      document.getElementById("userName").value = `${res.display_name} / ${res.email}`;
    });

    document.getElementById("toDashboard").addEventListener("click", () => {
      const u = Storage.getUser();
      if (!u) return alert("先にログインしてください");
      window.location.href = "record.html";
    });
  </script>
</body>
</html>
